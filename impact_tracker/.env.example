MANTIS_BASE_URL=https://mantis.newgen.co
PROXY_PORT=5100
VITE_API_BASE_URL=http://localhost:5100/api
# Cookie session signing key (required in production)
SESSION_SECRET=change-me-to-a-long-random-string
# Auth mode: token (MVP cookie session) | pubkit (stub — falls back to token until implemented)
# AUTH_MODE=token
